package com.recipe.app.service;

import org.springframework.stereotype.Service;
import org.springframework.web.reactive.function.client.WebClient;

import lombok.Builder;
import lombok.RequiredArgsConstructor;
import lombok.Value;

@Service
@Builder
@RequiredArgsConstructor
public class RecipeRecommendationService {
	private final WebClient webClient;  // 비동기 HTTP 클라이언트
	
	// application.yml에 제미나이 주소 들어갈 주소 넣기
	public RecipeRecommendationService(@Value("${python.geminai.service.url}") String pythonGeminiServerUrl) {
		this.webClient = WebClient.builder()
				.baseUrl(pythonGeminiServerUrl)
				.build();
	}
}
